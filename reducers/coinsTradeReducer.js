const coinsTradeReducer = (state = {}, action) => {
  switch (action.type) {
    case "LOAD_COINS":
      return (state = payload);
    default: {
      return state;
    }
  }
};

export default coinsTradeReducer;
